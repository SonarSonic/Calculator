buildscript {
    repositories {
        jcenter()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.1-SNAPSHOT'
    }
}
apply plugin: 'net.minecraftforge.gradle.forge'

version = "${minecraft_version}-${mod_version}"
group= "sonar.calculator.mod"
archivesBaseName = "Calculator"

minecraft {
version = "${forge_version}"
runDir = "run"
mappings = "stable_20"
}
repositories {
    mavenCentral()
    maven {
	name = "buildcraft"
	url = "http://mod-buildcraft.com"
    }
    ivy {
        name "BuildCraft"
        artifactPattern "http://www.mod-buildcraft.com/releases/BuildCraft/[revision]/[module]-[revision]-[classifier].[ext]"
    }
}

dependencies {
compile files('lib/Sonar-Core-1.8.9-2.0.0-dev.jar')
compile files('lib/jei_1.8.9-2.28.9.176.jar')
compile files('lib/StorageDrawers-1.8.9-2.6.3.jar')
compile files('lib/Waila-1.6.0-B3_1.8.8.jar')
}

processResources
{
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
                
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
        
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}

